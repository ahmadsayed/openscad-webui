{"status":"done","phase":"","code":"include <module.scad>\n\ncube_size = 20;\nhole_radius = 5;\nfacets = 16;\nball_radius = hole_radius * 0.8;\nrounding_radius = min(cube_size/4, hole_radius*1.5);\n\ndifference() {\n    rounded_cube([cube_size, cube_size, cube_size], rounding_radius, facets=facets);\n    rotate([90,0,0])\n    cylinder(h=cube_size+0.01, r=hole_radius, center=true, $fn=facets);\n}\nsphere(r=ball_radius, $fn=facets);","success":true}